<Window x:Class="CalcSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalcSharp"
        mc:Ignorable="d"
        Title="CalcSharp" Height="500" Width="320"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <!-- ✅ Resources FIRST before the Grid -->
    <Window.Resources>
        <Style x:Key="BaseBtn" TargetType="Button">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="50">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NumBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#3A3A54"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="FuncBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#4A4A6A"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="OpBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#F5A623"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="EqualBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#F5A623"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Display -->
        <Border Grid.Row="0" Background="#2A2A3E" CornerRadius="12" Padding="16">
            <StackPanel VerticalAlignment="Bottom">
                <TextBlock x:Name="ExpressionText"
                           Text=""
                           Foreground="#6B6B8A"
                           FontSize="14"
                           HorizontalAlignment="Right"/>
                <TextBlock x:Name="DisplayText"
                           Text="0"
                           Foreground="White"
                           FontSize="36"
                           FontWeight="Light"
                           HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0 -->
            <Button Content="C"   Grid.Row="0" Grid.Column="0" Style="{StaticResource FuncBtn}" Click="OnClear"/>
            <Button Content="+/-" Grid.Row="0" Grid.Column="1" Style="{StaticResource FuncBtn}" Click="OnNegate"/>
            <Button Content="%"   Grid.Row="0" Grid.Column="2" Style="{StaticResource FuncBtn}" Click="OnPercent"/>
            <Button Content="÷"   Grid.Row="0" Grid.Column="3" Style="{StaticResource OpBtn}"   Click="OnOperator" Tag="/"/>

            <!-- Row 1 -->
            <Button Content="7" Grid.Row="1" Grid.Column="0" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="8" Grid.Row="1" Grid.Column="1" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="9" Grid.Row="1" Grid.Column="2" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="×" Grid.Row="1" Grid.Column="3" Style="{StaticResource OpBtn}"  Click="OnOperator" Tag="*"/>

            <!-- Row 2 -->
            <Button Content="4" Grid.Row="2" Grid.Column="0" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="5" Grid.Row="2" Grid.Column="1" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="6" Grid.Row="2" Grid.Column="2" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="−" Grid.Row="2" Grid.Column="3" Style="{StaticResource OpBtn}"  Click="OnOperator" Tag="-"/>

            <!-- Row 3 -->
            <Button Content="1" Grid.Row="3" Grid.Column="0" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="2" Grid.Row="3" Grid.Column="1" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="3" Grid.Row="3" Grid.Column="2" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="+" Grid.Row="3" Grid.Column="3" Style="{StaticResource OpBtn}"  Click="OnOperator" Tag="+"/>

            <!-- Row 4 -->
            <Button Content="0"  Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource NumBtn}" Click="OnNumber"/>
            <Button Content="."  Grid.Row="4" Grid.Column="2" Style="{StaticResource NumBtn}" Click="OnDecimal"/>
            <Button Content="="  Grid.Row="4" Grid.Column="3" Style="{StaticResource EqualBtn}" Click="OnEquals"/>
        </Grid>
    </Grid>

</Window>